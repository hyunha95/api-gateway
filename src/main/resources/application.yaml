server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      # 서비스 디스커버리 기반 자동 라우팅 활성화
      # CORS는 CorsConfig.java에서 처리
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
    registry-fetch-interval-seconds: 5
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 10

# Actuator 엔드포인트
management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
  endpoint:
    health:
      show-details: always

# Auth0 인증 설정
auth:
  issuer: ${AUTH0_ISSUER_URI:https://dev-h73kda6a4tz25t6y.us.auth0.com/}
  audience:
    shop: ${AUTH0_AUDIENCE_SHOP:http://localhost:9090}
    cms: ${AUTH0_AUDIENCE_CMS:http://localhost:18080}

# 디버깅용 로깅
logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.cloud.netflix.eureka: DEBUG
